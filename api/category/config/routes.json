{
  "routes": [
    {
      "method": "GET",
      "path": "/product",
      "handler": "category.getProduct",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/category",
      "handler": "category.getCategory",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product/:product_code",
      "handler": "category.getProductOne",
      "config": {
        "policies": []
      }
    }
  ]
}
